flask>=3.0.0
requests>=2.31.0
python-dateutil>=2.8.2
# Optional — email alerts work with stdlib smtplib (no extra package needed)
# Optional — for AbuseIPDB integration
# requests already included above
